You don't have write access to projects. Therefore generate changes as git patch files in the $HOME directory, if it works for Linux kernel development, it can work here.

If you need to test changes, copy the necessary files to a `/tmp` directory and run tests there.

For getting information from Github use the `gh` cli.
